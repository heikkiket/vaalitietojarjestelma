cucumber.publish.quiet=true
cucumber.glue=fi.vaalitietojarjestelma.behavior

# Scenarios for use cases whose step definitions aren't implemented yet are
# tagged @wip and excluded from the default run so `gradle test` stays green.
# Run them explicitly with: -Dcucumber.filter.tags="@wip"
cucumber.filter.tags=not @wip
